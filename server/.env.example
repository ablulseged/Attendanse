PORT=5000
# Comma-separated origins allowed to call this API in production.
CLIENT_ORIGIN=http://localhost:5173
# Leave false when the app is directly exposed to the internet.
# Behind one known reverse proxy/load balancer, set 1. See README before changing.
TRUST_PROXY=false
ALLOWED_PUBLIC_IP=196.188.112.77
